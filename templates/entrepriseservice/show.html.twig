{% extends 'base.html.twig' %}

{% block title %}Entrepriseservice{% endblock %}

{% block body %}
    <h1>Entrepriseservice</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Reference</th>
                <td>{{ entrepriseservice.reference }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ entrepriseservice.libelle }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ entrepriseservice.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('entrepriseservice_index') }}">back to list</a>

    <a href="{{ path('entrepriseservice_edit', {'reference': entrepriseservice.reference}) }}">edit</a>

    {{ include('entrepriseservice/_delete_form.html.twig') }}
{% endblock %}
