{% extends 'base.html.twig' %}

{% block title %}Participantfonction{% endblock %}

{% block body %}
    <h1>Participantfonction</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Fonctionreference</th>
                <td>{{ participantfonction.fonctionreference }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ participantfonction.libelle }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ participantfonction.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('participantfonction_index') }}">back to list</a>

    <a href="{{ path('participantfonction_edit', {'fonctionreference': participantfonction.fonctionreference}) }}">edit</a>

    {{ include('participantfonction/_delete_form.html.twig') }}
{% endblock %}
