{% extends 'base1.html.twig' %}


{% block body %}
	<h1>Salonsalle</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Sallereference</th>
				<td>{{ salonsalle.sallereference }}</td>
			</tr>
			<tr>
				<th>Editionrefrence</th>
				<td>{{ salonsalle.editionrefrence }}</td>
			</tr>
			<tr>
				<th>Libelle</th>
				<td>{{ salonsalle.libelle }}</td>
			</tr>
			<tr>
				<th>Description</th>
				<td>{{ salonsalle.description }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('salonsalle_index') }}">back to list</a>

	<a href="{{ path('salonsalle_edit', {'sallereference': salonsalle.sallereference}) }}">edit</a>

	{{ include('salonsalle/_delete_form.html.twig') }}
{% endblock %}
