{% extends 'base.html.twig' %}

{% block title %}Entreprisechiffreaffaire{% endblock %}

{% block body %}
    <h1>Entreprisechiffreaffaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Chiffreaffairereference</th>
                <td>{{ entreprisechiffreaffaire.chiffreaffairereference }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ entreprisechiffreaffaire.libelle }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ entreprisechiffreaffaire.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('entreprisechiffreaffaire_index') }}">back to list</a>

    <a href="{{ path('entreprisechiffreaffaire_edit', {'chiffreaffairereference': entreprisechiffreaffaire.chiffreaffairereference}) }}">edit</a>

    {{ include('entreprisechiffreaffaire/_delete_form.html.twig') }}
{% endblock %}
