{% extends 'base.html.twig' %}

{% block title %}Entreprisenombreemploye{% endblock %}

{% block body %}
    <h1>Entreprisenombreemploye</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Reference</th>
                <td>{{ entreprisenombreemploye.reference }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ entreprisenombreemploye.libelle }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ entreprisenombreemploye.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('entreprisenombreemploye_index') }}">back to list</a>

    <a href="{{ path('entreprisenombreemploye_edit', {'reference': entreprisenombreemploye.reference}) }}">edit</a>

    {{ include('entreprisenombreemploye/_delete_form.html.twig') }}
{% endblock %}
