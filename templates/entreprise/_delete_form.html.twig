<form method="post" action="{{ path('entreprise_delete', {'reference': entreprise.reference}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ entreprise.reference) }}">
    <button class="btn">Delete</button>
</form>
