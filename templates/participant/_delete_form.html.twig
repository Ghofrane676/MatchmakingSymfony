<form method="post" action="{{ path('participant_delete', {'participantreference': participant.participantreference}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ participant.participantreference) }}">
    <button class="btn">Delete</button>
</form>
