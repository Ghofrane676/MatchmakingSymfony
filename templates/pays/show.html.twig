{% extends 'base.html.twig' %}

{% block title %}Pays{% endblock %}

{% block body %}
    <h1>Pays</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Reference</th>
                <td>{{ pay.reference }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ pay.libelle }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ pay.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pays_index') }}">back to list</a>

    <a href="{{ path('pays_edit', {'reference': pay.reference}) }}">edit</a>

    {{ include('pays/_delete_form.html.twig') }}
{% endblock %}
